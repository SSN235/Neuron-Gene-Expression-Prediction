{
  "name": "ngep-validator",
  "version": "1.0.0",
  "description": "Free NGEP validation tool on Cloudflare Workers",
  "main": "wrangler-worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "pages": "wrangler pages deploy frontend/"
  },
  "keywords": ["ngep", "validation", "cloudflare", "workers"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/wrangler": "^3.45.0",
    "@cloudflare/workers-types": "^4.20240814.0",
    "typescript": "^5.3.3"
  }
}
